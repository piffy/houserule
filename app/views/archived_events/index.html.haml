%h1 Listing archived_events

%table
  %tr
    %th
    %th
    %th

  - @archived_events.each do |archived_event|
    %tr
      %td= link_to 'Show', archived_event
      %td= link_to 'Edit', edit_archived_event_path(archived_event)
      %td= link_to 'Destroy', archived_event, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Archived event', new_archived_event_path
