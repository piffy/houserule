=provide(:title, "Eventi di #{@user.name}")
%h1
  Elenco eventi di
  =@user.name


=render "index_listing" , :path => "users"


%br

= link_to "Profilo di #{@user.name}" , user_path(@user), :class =>"btn"
= link_to "Elenco eventi" , events_path, :class =>"btn"